spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: false
        format_sql: false
#      javax:
#        persistence:
#          schema-generation:
#            scripts:
#              action: create
#              create-target: create.sql

app:
  scheduler:
    retention-days: 30   # 세션/스트리머 집계 시 기준이 되는 지난 날 수
    batch-size: 1000     # 페이지당 처리할 데이터 개수
    cron: "0 0 12 * * *" # 크론 표현식

logging:
  level:
    root: info
    org.hibernate.SQL: info
    org.hibernate.type.descriptor.sql.BasicBinder: trace
    org.hibernate.orm.jdbc.bind: trace

management:
  endpoints:
    web:
      exposure:
        include: "*"

processor:
  url: ${PROCESSOR_BASE_URL}

